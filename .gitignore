# Godot 4
.godot/
android/
export.cfg
# export_presets.cfg IS committed: a build anyone can download should be
# reproducible from the repository rather than from one machine's editor.
*.translation

# Build output
build/
dist/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Catalog build intermediate (regenerated by tools/build_catalog.sh)
build/

# Godot import metadata for the documentation screenshots is not needed:
# docs/.gdignore keeps the engine out of that folder entirely.
docs/**/*.import
